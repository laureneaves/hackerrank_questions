# Given a year, determine whether it is a leap year. If it is a leap year, return the Boolean True, otherwise return False.
def is_leap(year):
    leap = False

# if year is divisible by 4 and 400, it is a leap year. If it's divisible by 100, it's not a leap year. 

    if (year % 4 == 0) and (year % 100 != 0 or year % 400 == 0):
        leap = True
    
    return leap

year = int(raw_input())
